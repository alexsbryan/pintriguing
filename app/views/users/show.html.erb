<h1><%= "#{@user.first_name} #{@user.last_name}" %></h1>

<h1>Boards</h1>
<% @boards.each do |board| %>
<h3><%= board.name %></h3>
<ul>
  <% board.pins.each do |pin| %>

    <li><%= pin.title %></li>

  <% end %>

</ul>
<br>
<% end %>